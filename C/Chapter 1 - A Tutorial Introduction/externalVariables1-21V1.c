#include <stdio.h>

#define TAB_SIZE 4 // Columns per tab

/** DETAB: replace spaces with a proper number of tabs */

int main()
{
	char c;

	int nblanks, charp;

	while ((c = getchar) != EOF) {
		if (c = ' ') {
			++nblanks;
		}
	}
}

