#Choosing a distribution

Use CentOS as first option, and only use Debian/Ubuntu for specific lessons such as APT-Get

Small differences:

1. Ubuntu has different `usemod`.
2. Ubuntu has different structure for some logs.

Can you use your own VM?
-> YES, but the cloud server is quit handy to use.
